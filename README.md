This project simulates a Smart Traffic Management System
for a smart city. It demonstrates the OOP principle of
"Polymorphism" — where every traffic device receives
the same command activate() but each performs a
completely different task.
